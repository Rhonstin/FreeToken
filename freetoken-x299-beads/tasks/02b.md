# 02b. Закріпити checkpoint Qwen3.8 та бюджет RAM/VRAM — виконання та приймання

Версія плану: 2. Стара задача: 02.

## Вхідні умови

config.json; tensor metadata; target/draft shapes/dtypes; PLE extents; current Beads status.

## Контракт

Byte ledger: unique storages, fixed/variable, host/device, peak/steady; shared tensors не дублювати.

## Покрокове виконання

1. Прочитати завершений contract задачі a та перевірити evidence SHA/hardware fingerprint.
2. Виконати базову діагностику/experiment за contract. Для optimization без bottleneck evidence завершити no-change ADR, не писати speculative code.
3. Змінити лише узгоджені interfaces/files. Інший subsystem виявився необхідним — створити blocker bead, не розширювати scope мовчки.
4. Запустити positive і negative fixtures із задачі a та релевантні існуючі regression tests. Для bug спочатку red на baseline, потім green.
5. Запустити paired uninstrumented A/B за RUNBOOK.md; raw rows не редагувати. OOM/error/skipped окремо, а не в mean.
6. Перевірити rollback до baseline/default і повторний запуск. Заповнити result.json, no-change ADR за потреби.
7. Закрити bead тільки після GATES.md. У handoff вказати exact command, exit code, artifact SHA, limitations і actual next ready ID.

## Обов’язкові негативні сценарії

- відсутні mtp weights.
- mixed formats.
- FTW посилається на відсутній PLE.
- peak RAM > available.
- невідомий token template.

## Stop conditions

Зупинити запуск, якщо metadata load не узгоджується з checkpoint. Не конвертувати модель або format автоматично, щоб приховати fit failure.

## Артефакти

У `evidence/x299/02b/`: contract.json, commands.json, result.json і raw logs. Для a result.outcome=contract_ready; для b implemented/no_change/blocked. Actual raw measurement paths мають існувати. Усі статуси в Beads.

## Конкретні команди

```bash
bd show <actual-bead-id>
uv run pytest --collect-only -q <existing-test-paths-from-contract>
uv run pytest -q <existing-test-paths-from-contract> -m "not slow"
```

Кутові параметри — не runnable literals: задача a замінює їх реальними знайденими шляхами. Для inventory/benchmark-only задач pytest=N/A із reason, а acceptance перевіряється даними; це не test passed. Повні executable argv зберігати в commands.json.

## Критерій закриття цієї фази

Усі критерії нижче та GATES.md пройдені; no-change має виміри й причину; missing hardware не вважається no-change.

## Детальна постановка батьківської задачі

# 02. Закріпити checkpoint Qwen3.8 та бюджет RAM/VRAM

## Робота

Визначити exact HF/local checkpoint revision, architecture qwen4_exp, tokenizer/template, target/draft expert formats, dense formats, PLE table і FTW provenance. Порахувати steady і peak RAM: expert banks, PLE, pinned staging, conversion/load temporaries, MTP banks та OS reserve. Перевірити поточний fork SHA і Beads, не дублювати вже виконані KV/MTP задачі. Залишити Linux/WSL відмінності явними.

## Критерії приймання

Модель запускається без постійного swap/major faults. Таблиця bytes походить із tensor/checkpoint metadata, а не розміру архіву. Pinned PLE дозволений тільки якщо fit доведено. Якщо weights не вміщуються — checkpoint-selection gate з quality baseline, а не мовчазна підміна моделі. Availability MTP weights перевірена.

## Точки інтеграції

- python/freetoken/models/qwen4_exp/config.py
- python/freetoken/models/qwen4_exp/weight.py
- python/freetoken/models/qwen4_exp/mtp.py
- python/freetoken/moe/host_banks.py
- python/freetoken/engine/config.py

## Blocking dependencies

01

## Evidence та handoff

Exact commands, revisions, hardware/checkpoint, raw result paths, changed files, limitations. GPU-робота без target hardware — blocked. Для задач 09–14/16–19 допустимий обґрунтований no-change ADR після вимірів; він не називається реалізованим speedup. Статус ведеться в Beads після імпорту.
