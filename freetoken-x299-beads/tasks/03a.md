# 03a. Розширити наявний benchmark під decode latency — контракт і fixtures

Версія плану: 2. Стара задача: 03.

## Вхідні умови

paired BF16/no-spec baseline; actual tokenizer counts; server/client timestamps.

## Контракт

report schema_version; raw rows immutable; numerator/denominator documented; no fabricated per-token timing.

## Покрокове виконання

1. Зібрати inputs нижче; невідомі поля залишити null із reason, не підставляти guesses.
2. Прочитати поточні symbols у вказаних файлах; порівняти з baseline SHA. За drift оновити contract, не застосовувати старі hunks.
3. Створити contract.json за CONTRACTS.md: scope, baseline, fields, invariants, cases, commands, expected outcomes.
4. Описати кожний negative case як fixture: given/when/then, exact assertions; valid/invalid cases розділити.
5. Зібрати baseline/reproduction evidence до production code edit. Hardware відсутній — заблокувати лише необхідну hardware частину.
6. Зафіксувати bounded candidate set, numerical tolerances і performance metric до вимірів. Передати артефакти задачі b.

## Обов’язкові негативні сценарії

- SSE chunk містить кілька tokens.
- EOS завершує раніше.
- timeout.
- сервер падає.
- output порожній.
- usage відсутній.

## Stop conditions

Не змішувати incomplete/error rows із successful. Token timestamps тільки коли server надає їх; client chunk latency окремо.

## Артефакти

У `evidence/x299/03a/`: contract.json, commands.json, result.json і raw logs. Для a result.outcome=contract_ready; для b implemented/no_change/blocked. Actual raw measurement paths мають існувати. Усі статуси в Beads.

## Конкретні команди

```bash
bd show <actual-bead-id>
uv run pytest --collect-only -q <existing-test-paths-from-contract>
uv run pytest -q <existing-test-paths-from-contract> -m "not slow"
```

Кутові параметри — не runnable literals: задача a замінює їх реальними знайденими шляхами. Для inventory/benchmark-only задач pytest=N/A із reason, а acceptance перевіряється даними; це не test passed. Повні executable argv зберігати в commands.json.

## Критерій закриття цієї фази

Усі fixtures мають expected results, inputs перевірені, contract і baseline артефакти існують; production code не змінений.

## Детальна постановка батьківської задачі

# 03. Розширити наявний benchmark під decode latency

## Робота

Розширити benchmarks/bench_kv_quant.py і bench_decode_moe.py, не створювати конкуруючий framework. Основна метрика — committed output tokens/s одного запиту, TPOT та p50/p95 inter-token latency; TTFT окремо. Context 4K/16K/32K, 64K лише після fit; outputs 256/512, concurrency 1 основна, 2/4 secondary. Greedy fixed seeds, однакові tokenized prompts, мінімум 3 paired repeats після warmup, A/B або ABBA.

## Критерії приймання

JSON містить actual prompt/output token counts, server phase times і client times. SSE chunk не вважається token; speculative proposed tokens не рахуються output. Холодні load/JIT/prefix винесені окремо. Baseline команди відтворювані. PPL позначений blocked до незалежного teacher-forcing harness: server logprobs зараз немає.

## Точки інтеграції

- benchmarks/bench_kv_quant.py
- benchmarks/bench_decode_moe.py
- python/freetoken/benchmark/client.py
- python/freetoken/benchmark/perf.py

## Blocking dependencies

02

## Evidence та handoff

Exact commands, revisions, hardware/checkpoint, raw result paths, changed files, limitations. GPU-робота без target hardware — blocked. Для задач 09–14/16–19 допустимий обґрунтований no-change ADR після вимірів; він не називається реалізованим speedup. Статус ведеться в Beads після імпорту.
