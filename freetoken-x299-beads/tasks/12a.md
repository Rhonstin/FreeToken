# 12a. Bounded RAM row cache для дискового PLE — контракт і fixtures

Версія плану: 2. Стара задача: 12.

## Вхідні умови

row-ID locality, exact source bytes, cold/warm random read latency.

## Контракт

Immutable key=(source identity, table/head identity, row ID); bounded byte LRU; no unbounded metadata.

## Покрокове виконання

1. Зібрати inputs нижче; невідомі поля залишити null із reason, не підставляти guesses.
2. Прочитати поточні symbols у вказаних файлах; порівняти з baseline SHA. За drift оновити contract, не застосовувати старі hunks.
3. Створити contract.json за CONTRACTS.md: scope, baseline, fields, invariants, cases, commands, expected outcomes.
4. Описати кожний negative case як fixture: given/when/then, exact assertions; valid/invalid cases розділити.
5. Зібрати baseline/reproduction evidence до production code edit. Hardware відсутній — заблокувати лише необхідну hardware частину.
6. Зафіксувати bounded candidate set, numerical tolerances і performance metric до вимірів. Передати артефакти задачі b.

## Обов’язкові негативні сценарії

- same row different checkpoint.
- extent boundary.
- duplicate rows.
- hash collision.
- cache size zero.
- read error.
- eviction during fill.

## Stop conditions

Cache disabled by default до evidence. Identity equality перевіряється повністю, не лише hash. Read errors propagate.

## Артефакти

У `evidence/x299/12a/`: contract.json, commands.json, result.json і raw logs. Для a result.outcome=contract_ready; для b implemented/no_change/blocked. Actual raw measurement paths мають існувати. Усі статуси в Beads.

## Конкретні команди

```bash
bd show <actual-bead-id>
uv run pytest --collect-only -q <existing-test-paths-from-contract>
uv run pytest -q <existing-test-paths-from-contract> -m "not slow"
```

Кутові параметри — не runnable literals: задача a замінює їх реальними знайденими шляхами. Для inventory/benchmark-only задач pytest=N/A із reason, а acceptance перевіряється даними; це не test passed. Повні executable argv зберігати в commands.json.

## Критерій закриття цієї фази

Усі fixtures мають expected results, inputs перевірені, contract і baseline артефакти існують; production code не змінений.

## Детальна постановка батьківської задачі

# 12. Bounded RAM row cache для дискового PLE

## Робота

C++ PLE store deduplicates rows within fill, але не кешує між fill. Спочатку trace row reuse та disk latency. Якщо є locality — bounded exact-row cache із checkpoint/extent identity; capacity від реально вільної RAM, не від усіх 104 GB. Порівняти disk, disk+cache і full pinned лише якщо fit. Cache packed source bytes для збереження decoder semantics.

## Критерії приймання

Repeated rows byte-identical, checkpoint change invalidates cache, deterministic eviction і hard memory cap. Miss/read error не підміняється нулями. Cold/warm/random-code workloads показують hit rate і TPOT; без gain код не вмикається. SSD/NVMe на машині не припускається.

## Точки інтеграції

- python/freetoken/kernel/csrc/ple_store/ple_store_ext.cpp
- python/freetoken/models/qwen4_exp/ple_disk.py
- tests/models/qwen4_exp/test_ple_disk.py

## Blocking dependencies

02, 04

## Evidence та handoff

Exact commands, revisions, hardware/checkpoint, raw result paths, changed files, limitations. GPU-робота без target hardware — blocked. Для задач 09–14/16–19 допустимий обґрунтований no-change ADR після вимірів; він не називається реалізованим speedup. Статус ведеться в Beads після імпорту.
